apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "scrape.fullname" . }}-test-connection"
  labels:
    {{- include "scrape.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "scrape.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
