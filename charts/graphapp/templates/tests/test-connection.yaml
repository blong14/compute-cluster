apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "graphapp.fullname" . }}-test-connection"
  labels:
    {{- include "graphapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "graphapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
