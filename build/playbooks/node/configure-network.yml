- name: Disable ufw
  become: yes
  become_method: sudo
  command: 'ufw disable'
- name: Copy
  become: yes
  become_method: sudo
  ansible.builtin.copy:
    src: /home/blong14/Developer/compute-cluster/build/etc/10-flannel.link
    dest: /etc/systemd/network